{
  "name": "part4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=development nodemon index.js",
    "test:watchAll": "NODE_ENV=testing NODE_OPTIONS=--experimental-vm-modules jest --config Tests/jest.config.js --watchAll --detectOpenHandles",
    "test:watch": "NODE_ENV=testing NODE_OPTIONS=--experimental-vm-modules jest --config Tests/jest.config.js --watch --detectOpenHandles",
    "test:withdb": "NODE_ENV=testing NODE_OPTIONS=--experimental-vm-modules jest --config Tests/jest.config.js --watch --runInBand --detectOpenHandles"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "lodash": "^4.17.21",
    "mongoose": "^7.4.1"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "eslint": "^8.46.0",
    "jest": "^29.6.2",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  }
}
